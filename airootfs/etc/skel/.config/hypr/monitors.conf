# Lin OS — monitor auto-config
# This file is regenerated on first login by lin-welcome if needed.
# Edit here to override auto-detection.
#
# Format: monitor=<name>,<resolution>,<position>,<scale>
# Use `hyprctl monitors` to list connected monitors.
